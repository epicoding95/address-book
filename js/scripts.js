$(document).ready(function() {
  $("form#addressinfo").submit(function(event) {
    event.preventDefault()
    var nameResponse=($("#name1").val());
    var addressResponse=($("#address1").val());
    var numResponse=($("#number1").val());

  });
});
